var minhaVariavel = 'minha variavel';

function myFunction(x, y) {

    return x + y;
}

let num = 2;

const num2 = 2;

let numbers = [1, 2, 3];

numbers.map(value => value * 2);

class Soma {

    somar(x, y) {

        return x + y;
    }
}

var string1 : string = 'KKKKKK';